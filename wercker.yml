box: elegantscipy/elegantscipy

build:
  steps:
    - pip-install
    - script:
        name: build book
        code: |
          mv /data/* ./data
          make zip
          mv *.zip $WERCKER_OUTPUT_DIR

  after-steps:
    - 1syo/gitter-notify@0.0.1:
        token: 3f461c40d53d840b3ebc
